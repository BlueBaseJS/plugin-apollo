import * as React from 'react';
import { Text } from 'react-native';

const App = () =>
      (
         <Text>
            It must be rendererd in ApolloProvider
        </Text>
      );

export default App;
